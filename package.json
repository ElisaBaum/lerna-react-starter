{
  "name": "root",
  "private": true,
  "scripts": {
    "lerna": "lerna bootstrap",
    "start": "lerna bootstrap && lerna run --parallel start",
    "build": "tslint -c tslint.json --project . && lerna bootstrap && lerna run build",
    "test": "lerna bootstrap && jest --env=jsdom",
    "lint": "tslint -c tslint.json --project ."
  },
  "devDependencies": {
    "@types/enzyme": "^3.1.15",
    "@types/jest": "^26.0.15",
    "@types/react": "^16.7.13",
    "@types/react-dom": "^16.0.11",
    "enzyme": "^3.8.0",
    "enzyme-adapter-react-16": "^1.7.1",
    "jest": "^23.6.0",
    "lerna": "^3.5.1",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-react": "^3.6.0",
    "typescript": "^3.2.1"
  },
  "dependencies": {
    "react": "^16.6.3",
    "react-dom": "^16.6.3"
  }
}
